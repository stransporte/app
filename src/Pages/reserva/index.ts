import { SPage, SPageListProps } from 'servisofts-component';

import root from './root';
// import carga from "./carga"

export default SPage.combinePages("/reserva", {
  // "": carga,
  "": root,

});
